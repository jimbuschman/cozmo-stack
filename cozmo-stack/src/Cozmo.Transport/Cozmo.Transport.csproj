<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <RootNamespace>Cozmo.Transport</RootNamespace>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\Cozmo.Protocol\Cozmo.Protocol.csproj" />
  </ItemGroup>
  <ItemGroup>
    <!-- The receive-error and manual-pump seams on ReliableTransport are driven directly by the transport tests. -->
    <InternalsVisibleTo Include="Cozmo.Protocol.Tests" />
  </ItemGroup>
</Project>
